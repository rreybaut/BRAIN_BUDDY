<div class="loginpage">
  <%= render "shared/logo" %>
  <div class="login_flex-A">
    <h2 class="subtitle">Connectez-vous</h2>
        <%= render "devise/shared/links" %>
  </div>
  <div class="login_flex-B">
    <div class="AnimatedBox_login">
      <div id="form_login">
        <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
          <div class="form-inputs">
            <%= f.input :email,
                        required: false,
                        autofocus: true,
                        input_html: { autocomplete: "email" } %>
            <%= f.input :password,
                        required: false,
                        input_html: { autocomplete: "current-password" } %>
            <%= f.input :remember_me, as: :boolean if devise_mapping.rememberable? %>
          </div>

          <div class="form-actions">
            <%= f.button :submit, "Connexion", id:"btn-new_user" %>
          </div>
        <% end %>

      </div>
    </div>
  </div>
  <div class="logos-container">
    <div class="logos">
      <%= image_tag "bb_micro.png", class: "A" %>
      <%= image_tag "bb_idee.png", class: "logos B" %>
    </div>
  </div>
</div>
